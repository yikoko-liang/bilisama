# BiliSama MVP 验证方案

版本：1.4（2026-08-28）

这份方案回答三件事：当前 MVP 到底支持什么；沙箱、真实直播间和业务体验分别怎么测；
什么结果算通过。主播实际操作的两套测试集在 `config/testsets/functional.json` 和
`config/testsets/business.json`，启动桌面 UI 后从「测试」页逐条运行。`tests/fixtures/mvp/`
保留更细的边界/压力回放数据，不是主播执行功能验收的入口。

## 1. 结论先行

MVP 应按四道门依次验证：自动回归、人工沙箱、真实房间影子运行、受控开播。前一道不过，
不要用后一道的偶然好效果掩盖问题。

当前实现有三个容易误解的边界：

1. `--room` 接的是 B 站直播事件，不会自动拉取直播播放器的音轨。普通开播时语音来自
   `--input-device` 指定的本机音频输入；「测试 → 直播 Mock」可以共享 Chrome 标签页音轨，
   用浏览器音频临时替代麦克风，同时接入所填房间的真实事件。当前音频模型仍然看不懂共享
   画面。操作方法见 `docs/runbook.md` 的「直播 Mock」章节。
2. 事件模型包含弹幕、礼物、SC、上舰、VIP 进房、普通进房、关注、点赞、分享和房间状态
   十类，见 `src/bilisama/ingest/events.py:26`。关注、点赞、分享和房间状态目前只进入事件流
   和记忆，不会生成回复；普通进房只有达到批量欢迎门槛才开口，见
   `src/bilisama/director/intents.py:266`。
3. “抗干扰”不是说话人分离。当前能验证的是服务端判停、耳机条件下的打断，以及外放时的
   播放期间闭麦。外放闭麦会牺牲 AI 播放期间的打断能力，代码已明确写出这个取舍，见
   `src/bilisama/dev_talk.py:371`。多人连麦、电视人声和高音量 BGM 属于边界场景。

## 2. 验证目标和不在本轮证明的事情

### 2.1 本轮要证明

- 语音链路能连续对话，主播可以及时打断，常见非人声噪声不会造成失控回复。
- 直播行为能正确解析、去重、入库、筛选和调度，付费事件不丢、不重复答谢。
- 冷场话题、批量欢迎、常客记忆、下播蒸馏、紧急叫停、UI 和配置编辑能按现有设计工作。
- 在六类候选内容里，伴播回复相关、诚实、短、能让主播接得住，不假装看见代码或直播画面。
- 洪峰、断线、匿名身份、恶意输入和长场运行不会让系统失控。

### 2.2 本轮不能只靠沙箱证明

- 观众停留、弹幕率、关注率是否提升。沙箱只能证明体验有机会产生价值，不能证明真实业务
  增量。业务增量需要后续真实开播的对照数据。
- 多人说话时准确识别主播。当前没有声纹或说话人分离。
- 理解屏幕里的代码、节点图、机器人动作或生成结果。MVP 上下文来自主播语音、直播事件和
  记忆，不来自画面。
- B 站直播音轨自动拉流。远程验证依赖外部音频路由，不能在报告里写成产品能力。

## 3. 当前功能基线

| 能力 | 当前规则 | 验收重点 | 代码依据 |
|---|---|---|---|
| 主播语音 | 主播说话权最高 | 连续对话、判停、打断、恢复 | `src/bilisama/director/intent.py:24-45` |
| SC | 优先级 80，付费事件可重排 | 不丢、不重复；回复未输出时能抢占低优先级，已经开口时排队 | `src/bilisama/director/intent.py:29`、`src/bilisama/director/scheduler.py` |
| 大额礼物 | 1000 电池起，优先级 70；中额从 100 电池起 | 连击聚合、主播打断后重排 | `config/bilisama.toml:110-111` |
| 上舰 | 优先级 65 | 即时答谢、重连去重 | `src/bilisama/director/intent.py:32` |
| VIP 进房 | 优先级 50，每场每人一次 | 当前舰队用户或本房粉丝牌等级 ≥5 按名欢迎；按等级给情绪价值并带出直播主题 | `src/bilisama/app.py`、`src/bilisama/director/intents.py` |
| 普通进房 | 按房间活跃度等待 1/2/4 秒合并，繁忙时抑制 | 单人也及时欢迎；进房后立即发弹幕不重复欢迎；不播报人数 | `src/bilisama/event_pacing.py`、`src/bilisama/ingest/bilibili/selector.py` |
| 弹幕 | medium 按冷清/稀疏/活跃/繁忙使用 1/2/4/8 秒窗口；主播说话时暂存最多 5 个窗口胜者，停下后只释放最优一条 | 冷清房快速接住，繁忙房只挑少量高价值内容；不因同一用户刚被回复而屏蔽后续追问 | `src/bilisama/event_pacing.py`、`src/bilisama/ingest/bilibili/selector.py` |
| 冷场话题 | medium 按直播事件氛围等待 30/60/120 秒，繁忙时关闭 | 只看直播事件冷场，主播语音只提供话题素材且始终可抢回话权 | `src/bilisama/event_pacing.py`、`src/bilisama/proactive.py` |
| 关注/点赞/分享 | 默认开关关闭，当前无语音意图 | 正确显示和入库，不误发声 | `config/bilisama.toml:117-127`、`src/bilisama/director/intents.py:266` |
| 记忆/蒸馏 | 每 40 个事件滚动整理；生长层默认 off | 三态语义、锚点不漂移、退出收尾 | `config/bilisama.toml:139` |
| 洪峰保护 | 弹幕 80 条/秒、进房 40 条/秒解析预算；付费事件始终解析 | 普通事件可采样，付费项零丢失 | `src/bilisama/ingest/bilibili/source.py:72-77` |

优先级的完整顺序是：主播语音 100 > SC 80 > 大额礼物 70 > 上舰 65 > VIP/中额礼物 50 >
后台结果 40 > 弹幕/小额礼物/批量欢迎 30 > 主动话题 10。同级排队；回复尚未输出时，只有严格
更高的优先级才能抢占。一旦已经输出，直播事件之间只排队，不截断当前句子。来源见
`src/bilisama/director/intent.py:24-45`、`src/bilisama/director/scheduler.py`。

话痨度现在是动态策略上的相对档位，不是固定冷却：Low 的普通互动容量为 1、时间系数 1.35；
Medium 为 2 和 1.0；High 为 3 和 0.7。普通弹幕、普通进房、主动话题共享预算，礼物、SC、
上舰和 VIP 绕过预算。主播语音不进入这套统计，也不受预算限制，见
`src/bilisama/event_pacing.py`。

## 4. 四道测试门

| 阶段 | 环境 | 目的 | 通过后才能做什么 |
|---|---|---|---|
| G0 自动回归 | Mock Realtime + ReplaySource | 确认协议、调度、事件、记忆和测试集结构没坏 | 人工用麦克风 |
| G1 人工沙箱 | 真实 DashScope + 电脑麦克风 + 虚构直播行为 | 验证人耳可感知的延迟、打断、自然度和领域效果 | 连真实房间影子运行 |
| G2 真实房间影子运行 | 真实房间事件 + 真实音频，AI 只进测试耳机 | 验证平台事件、匿名/登录态、重连和长场稳定 | 小流量受控开播 |
| G3 受控开播 | AI 声音送进 OBS，有主播随时紧急叫停 | 验证真实节目效果和止损机制 | 扩大内测 |

G3 不属于本次“无需真实直播间”的业务沙箱，但必须保留为以后证明业务增量的步骤。

## 5. 统一记录方式

每次运行建立一个测试批次，至少保存：

- 日期、测试人、commit、模型名、配置文件哈希、话痨度、输入/输出设备编号、是否耳机。
- 场景 ID、开始时间、原始输入、主播语音转写、AI 完整回复、调度结论和最终状态。
- `speech_stopped` 到首个可听音频的延迟，打断开口到本地停播的延迟。
- 事件收取数、选择器 delivered/skips、去重数、付费事件漏接数、重复答谢数。
- 人工评分、失败级别、复现次数、对应日志或录屏时间点。

动态模型输出不能只跑一次。功能语音用例按用例规定的次数跑；每位候选的四张业务卡各跑三次，
共 12 个样本，两名评分人独立评分。有 2 分以上分歧时由第三人复核。

### 5.1 失败级别

| 级别 | 定义 | 处理 |
|---|---|---|
| P0 | 泄露密钥、无法紧急止声、危险建议、付费事件静默丢失 | 立即停止真实开播 |
| P1 | 进程崩溃、连续自激、频繁误打断、付费重复答谢、角色被弹幕劫持 | G1/G2 不通过，修复后全量回归 |
| P2 | 偶发答非所问、节奏生硬、冷场话题无聊、普通事件漏回 | 记录频率，影响业务门槛 |
| P3 | 文案、UI 展示和非阻塞体验问题 | 可带已知问题进入小范围内测 |

## 6. 功能验收：沙箱

### 6.1 环境准备

1. 用仓库根目录的 `start_bilisama.command` 启动 DashScope、麦克风和桌面 UI。
2. 戴耳机；先确认麦克风输入设备是主播实际使用的设备。
3. 保持默认 `chattiness = "medium"`。阈值场景除非用例明确要求，不改配置。
4. 打开桌面 UI 的「系统」「聊天记录」「运行日志」页，同时录屏和录系统音频。
5. 业务场景使用测试账号名，不使用真实观众隐私数据。

### 6.2 语音实时对话

机器可读测试卡为 `F-SBX-VOICE-01` 至 `F-SBX-VOICE-07`，重点矩阵如下：

| 方向 | 输入 | 重复 | 主要门槛 |
|---|---|---:|---|
| 基本连续对话 | 三个承接上下文的问题 | 5 组 | 15 轮均有且只有一次完成回复 |
| 打断 | AI 说约 1 秒后主播纠正 | 20 | 至少 19 次成功，本地止声不超过 500ms |
| 句中停顿 | 300/700/1200ms 后继续 | 每档 10 | 300/700ms 合计误抢答不超过 1 次 |
| 机械噪声 | 键盘、鼠标、敲桌、风扇 | 10 分钟 | 误回复 0 次，误起声不超过 2 次且能恢复 |
| BGM | 约 -30/-20/-10dB | 每档 10 | 低档成功率至少 90%，中档至少 80%，高档单报 |
| 旁人声 | 旁人、主播、重叠三段 | 每类 5 | 不伪造说话人身份；失败可紧急止损 |
| 回声 | 耳机与外放闭麦对照 | 各 10 | 耳机可打断；外放闭麦不自激，明确其不能打断 |

建议口播覆盖短句、长句、数字、英文缩写、否定纠正和半句改口，例如：

- “我现在演示的是图生视频，不是文生视频，先记住这个区别。”
- “这个节点需要十六 G 显存——等一下，我说错了，八 G 也能走低显存模式。”
- “先别回答，我还没说完……我想问的是为什么这里会显存溢出？”
- “AI、AIGC、ControlNet、LoRA 和 VRAM 分别用中文语境说一遍。”

“识别到了词”不等于通过。回复必须针对主播最终表达的意图，不能把被纠正的半句继续当结论。

### 6.3 直播行为

| 场景 | 测试卡/夹具 | 预期 |
|---|---|---|
| 全事件覆盖 | `F-SBX-EVENT-01` / `mvp/functional_all_events.jsonl` | 十类都能解析；feed-only 类型不误发声 |
| 新观众欢迎 | `F-SBX-EVENT-02` / `presence.jsonl` | 冷清时约 1 秒欢迎；短时多人合并；发弹幕者取消重复欢迎 |
| VIP 欢迎 | `F-SBX-EVENT-03` / `presence.jsonl` | 同一 VIP 每场按名一次 |
| 礼物答谢 | `F-SBX-EVENT-04` / `gift_combo.jsonl` | 分档正确，连击一次答谢 |
| SC | `F-SBX-EVENT-05` / `superchat_during_speech.jsonl` | 低优先级未开口时抢占；已经开口时排队；被主播打断后重排 |
| 弹幕 | `F-SBX-EVENT-06` / `event_flood.jsonl` | 动态窗口最多一位胜者；同一用户的新追问仍可竞争；主播说话时不扣预算、不启动有效期，停下后只释放最优候选；繁忙档预算收紧 |
| 冷场 | `F-SBX-EVENT-07` / `quiet_stream.jsonl` | medium 冷清档约 30 秒起题，繁忙档不主动起题 |

真人体验时，可以在 UI 注入框或终端输入普通弹幕、`/gift 名字 电池数`、
`/sc 名字 金额 内容`。完整 JSONL 由 `tests/fakes/replay.py` 的 ReplaySource 驱动，用于自动化和
组合测试；UI 注入框目前不伪造上舰、进房、关注、点赞、分享和房间状态。

### 6.4 其他已有功能

- `F-SBX-SAFE-01`：提示词注入、密钥索取和伪造角色。
- `F-SBX-SAFE-02`：AI 正在说且队列非空时紧急叫停。
- `F-SBX-MEM-01`：三场常客记忆，生长层 off/collect/on，锚点文件不漂移。
- `E-UI-01`：关闭和重开桌面 UI，不重启仍在工作的后端。
- `E-CONFIG-01`：可直播修改的字段立即生效，需要重启的字段只做标记。
- `E-FAIL-02`：无侧路模型时，主链路照常；主动话题由 Realtime 直接读共享上下文生成，蒸馏明确停用。

关注、点赞、分享当前默认不说，打开开关也没有对应语音意图。它们的正确验收结果是事件可见、
可入库、不会突然念出来；若产品希望它们发声，那是新增需求，不应把本次测试写成失败修复。

## 7. 功能验收：真实直播间

### 7.1 两种音频接法

主播本机联调是首选：BiliSama 直接采主播实际麦克风，`--room` 同时收房间事件。这样延迟最小，
也最接近最终使用方式。

旁路机只用于远程观察：把 B 站播放器的音频通过 BlackHole、Loopback 等虚拟声卡送入
`--input-device`，AI 输出必须走另一个设备或耳机，不能回送到输入总线。报告要保存音频路由图。

真实房间命令：

```bash
source path.sh
.venv/bin/bilisama dev-talk \
  --director \
  --provider dashscope \
  --model qwen-audio-3.0-realtime-flash \
  --room <房间号>
```

`dev-talk` 没有 `--mic` 参数；不传 `--wav` 时本来就是麦克风模式。登录态从 `path.sh` 的
`BILI_SESSDATA` 读取，禁止把值写进测试报告或仓库。

一键启动器可用环境变量带房间号：

```bash
BILISAMA_ROOM_ID=<房间号> ./start_bilisama.sh
```

### 7.2 顺序

1. `F-REAL-01`：用正在直播的短号连接，确认解析后的真实房间号和健康状态。
2. `F-REAL-02`：同房间做匿名/登录态对照，确认打码比例和常客能力差异。
3. `F-REAL-03`：主播本机 30 分钟，真实麦克风和事件同跑，AI 不进 OBS。
4. `F-REAL-04`：如确有远程旁路需求，再验证虚拟声卡路由。
5. `F-REAL-05`：测试账号逐项触发弹幕、礼物、SC；上舰和真实付费不要为了测试无谓消费，
   可以等待自然事件并与平台画面核对。
6. `F-REAL-06`：断网 20 秒后恢复，再切换一次网络。
7. `F-REAL-07`：在可控条件验证 SC 撤回。
8. `F-REAL-08`：AI 只进测试耳机，连续影子运行 60 分钟。

### 7.3 真实房间硬门槛

- 付费事件漏接 0，重复完整答谢 0，金额或用户名串线 0。
- 紧急叫停 100% 成功，不能出现关了还继续说完一段的情况。
- 连续 60 分钟无崩溃、无失控连续发言、无必须强退才能结束。
- 平台画面与本地事件流随机核对至少 30 条普通事件，记录匹配率和匿名比例。
- 断线恢复后 2 分钟内继续收到事件；恢复过程有可见状态，不允许默默失效。

## 8. 业务测试

### 8.1 业务问题

这轮业务沙箱不测“涨了多少停留”，只回答下面四个问题：

1. 主播忙于操作时，伴播能否接住值得回的弹幕？
2. 冷场时，它抛出的话题是否与刚才内容有关，主播愿不愿接？
3. 回复是否足够诚实，不假装看见画面、不编链接、不补主播没说的事实？
4. 它是否让节目更像两个人配合，而不是第二个主播抢话？

### 8.2 评分表

每个输出按 1–5 分评分：

| 维度 | 1 分 | 3 分 | 5 分 |
|---|---|---|---|
| 相关性 | 与当前话题无关 | 大方向相关但泛 | 精确接住当前步骤或问题 |
| 正确与诚实 | 编造事实或绝对保证 | 有模糊推断 | 事实有来源，未知明确说未知 |
| 时机 | 抢话或回答过期内容 | 能用但略生硬 | 空档进入、主播开口即让位 |
| 简洁 | 长篇讲课 | 稍长 | 一到两句，主播容易接 |
| 人设与语气 | 像客服或说教 | 基本自然 | 像熟悉主播的搭档 |
| 互动价值 | 终结话题 | 可继续也可不继续 | 自然引出主播或观众下一步 |

单个候选通过条件：六张卡各跑两次，72 个维度分的总平均至少 4.0；“正确与诚实”不得出现
P0/P1，低于 3 分的输出不超过 2 个；主播本人对“愿意在真实直播里听到这句”的是票至少 9/12。

### 8.3 候选画像和测试重点

| 候选 | 暂定内容 | 最适合验证 | 主要风险 | 测试卡 |
|---|---|---|---|---|
| AI代码侠土豆 | ComfyUI、AIGC 工作流、智能体实操 | 安装开场、显存排错、生成冷场、智能体付费问答、资料发布洪峰、密钥与版权 | 编造节点状态、链接和版权承诺 | `biz.tudou.*` 6 张 |
| BetterGI | 游戏视觉自动化、脚本、技术演示 | 新版安装、视觉排错、路线卡点、开源支持、游戏风险、Issue 洪峰 | 封号零风险承诺、把管理员权限说成万能 | `biz.bettergi.*` 6 张 |
| 清月已经不困了 | 按需求暂定为 AI 行业和科技评论 | 议题开场、突发消息、模型争论、SC 撤回、议题转场、传闻洪峰 | 候选链接公开内容与暂定方向不完全一致 | `biz.qingyue.*` 6 张 |
| 老师好我叫石同学 | 人形机器人、具身智能、开源研发 | 演示开场、异常急停、接线空档、上舰去重、原型边界、集中进房 | 假装看见实体动作、催促危险操作 | `biz.shitong.*` 6 张 |
| Ai_随风 | AI 动态漫、短剧、自动化制作 | 教程开场、渲染投票、一致性反馈、礼物连击、真人授权、首映洪峰 | 虚构生成结果、淡化肖像和音色授权 | `biz.suifeng.*` 6 张 |
| 小小小名不是小明 | 公开内容显示为模型评测、AI 编程、生成式演示 | 横评开场、方法争论、代码排错、结果付费、记忆建档、跨场回访 | 补全不存在的参数、夸大单次样本 | `biz.xiaoming.*` 6 张 |

候选方向的依据和风险：

- AI代码侠土豆的[个人站介绍](https://www.aitudou.cn/about)与需求给出的实操型 AI 方向一致。
- BetterGI 的[官方项目说明](https://github.com/babalae/better-genshin-impact)将它定义为基于计算机
  视觉的开源项目，业务样本集中在视觉、脚本、排错和风险表达。
- 老师好我叫石同学的[公开项目视频](https://www.bilibili.com/video/BV1MMMDzbELr)直接展示开源
  人形机器人研发。
- Ai_随风的[公开视频](https://www.bilibili.com/video/BV1P6DaYeE33/)和
  [动态漫流程](https://www.bilibili.com/list/3546731499228115?bvid=BV1h5SEB8EkG&oid=115627650057325)
  支持需求给出的方向。
- 小小小名不是小明的[公开评测视频](https://www.bilibili.com/video/BV1feK76eEm3/)显示大模型评测、
  AI 编程和生成式演示方向，因此补了一套暂定测试；首测前仍由主播确认。
- 清月已经不困了的精确 UID 能在[公开录播](https://www.bilibili.com/video/BV1hRd3YcEpM/)中对应上，
  但公开内容不足以支持“AI 行业/科技主播”这一判断。`B-QINGYUE-01` 是强制画像确认门，不能跳过。

### 8.4 每位候选的执行方式

1. 打开桌面面板的「测试」页，选择一位主播，按 `01` 到 `06` 运行他的六张卡。
2. 每张卡的「你要做」都给出要说的原句和时间；没有要求时不要临时补画面或日志信息。
3. 冷场卡按卡片标注的动态阈值等待；压力卡运行时按卡片要求继续说话并切换页签。
4. 小明的记忆建档和跨场回访必须连续运行，中间不清记忆、不重启。
5. 六张卡跑完后只重跑失败卡一次，不挑最好输出；两名评分人独立打分，再由主播给
   “愿不愿在真实直播里听到”二选一。

## 9. 边界和压力测试

### 9.1 优先级

`E-PRI-01` 使用 `mvp/priority_collision.jsonl` 在 600ms 内送入七个等级的事件。还要人工增加
两个外部条件：AI 正在回答普通弹幕；主播在 SC 开口后插话。优先级负责选择下一条回复，不能把
已经开始输出的直播事件回复切成半句；主播语音仍走独立的打断路径，付费事件被主播打断后不能丢。

必须检查：

- 回复尚未输出时，严格更高优先级才可抢占，同级排队。
- 回复已经输出后，SC、礼物、上舰、进房、弹幕和主动话题都排队，不互相截断。
- SC、礼物、上舰被主播打断后重排；过期普通弹幕不重排。
- 批量欢迎和普通弹幕同为 30，只能排队，不能互相打断。
- 主动话题尚未输出时为真实互动让位；已经开口后先说完当前一句。

### 9.2 压力档位

| 档位 | 输入 | 用例 | 通过条件 |
|---|---|---|---|
| 短洪峰 | 15–30 弹幕/秒，持续 10 秒 | `E-LOAD-01` | 无卡顿，窗口每次最多一位胜者 |
| 超预算 | 超过 80 弹幕/秒，持续 30 秒 | `E-LOAD-01` 扩展 | `shed` 增长，付费事件仍全部解析 |
| 进房潮 | 30 人/5 秒，再扩大到 100 人 | `E-LOAD-02` | 进入繁忙档，普通欢迎被抑制或至多一次，主播话权不受影响 |
| 礼物连击 | 同组合 50 帧，三个观众交错 | `E-LOAD-03` | 每组合一次答谢，金额守恒 |
| 低流量长场 | 四小时循环低速事件和冷场 | `E-LONG-01` | 资源不持续增长，末段质量不恶化 |

压力结果不能只写“没崩”。退出状态里的 `offered`、`delivered`、`skips`、`shed`、`deduped`、
事件循环延迟和付费事件对账都要保留。

### 9.3 身份、时效、故障和安全

- `E-DEDUP-01`：重连重复包、无 event_id、同秒不同礼物。
- `E-ID-01`：uid=0 但有 uid_hash，匿名观众不能被丢弃或合并。
- `E-TTL-01`：主播持续说 25 秒，普通弹幕过期，付费事件保留。
- `E-FAIL-01`：语音服务断线、会话槽被占、网络切换。
- `E-FAIL-02`：侧路模型缺失或超时时，主动话题走 Realtime 降级路径。
- `E-UI-01`：UI 关闭、重开和端口变化。
- `E-CONFIG-01`：直播可改字段和重启字段的行为一致性。
- `E-SAFE-01`：长文本、空白、控制字符、标签逃逸、密钥索取和非法 UI 帧。

## 10. 测试集说明

目录 `tests/fixtures/mvp/` 包含：

| 文件 | 用途 |
|---|---|
| `manifest.json` | 62 张人工/自动测试卡、六位候选画像、输入和通过条件 |
| `functional_all_events.jsonl` | 十类事件模型全覆盖 |
| `priority_collision.jsonl` | 同秒优先级和礼物分档 |
| `business_ai_code_tudou.jsonl` | ComfyUI、工作流、智能体和版权边界 |
| `business_bettergi.jsonl` | 视觉自动化、OCR、脚本、风险表达 |
| `business_qingyue_ai_news.jsonl` | 按需求暂定的 AI 行业新闻测试，带画像确认门 |
| `business_robotics_shi.jsonl` | 机器人研发、开源范围和实体安全 |
| `business_ai_video_suifeng.jsonl` | 动态漫工作流、一致性、授权和生成等待 |
| `business_xiaoming_model_eval.jsonl` | 模型评测、AI 编程、版本不确定性 |

同时复用原有八个夹具：匿名身份、事件洪峰、礼物连击、提示词攻击、进房潮、冷场、跨场常客和
说话中到达 SC。这一目录是底层自动回放的补充矩阵，不是主播在 UI 里逐张执行的业务卡。
其完整性由 `tests/unit/test_mvp_validation_set.py` 固定：测试卡 ID 不重复、引用的夹具存在、
时间单调、事件 ID 唯一、十类事件齐全、六位候选各有四张自动回放卡。主播实际执行的是
`config/testsets/business.json` 中每位 6 张、共 36 张业务卡，结构和 runner 由
`tests/unit/test_ui_test_runner.py` 固定。

运行测试集结构和语义检查：

```bash
.venv/bin/pytest -q tests/unit/test_mvp_validation_set.py
```

运行与本方案直接相关的现有自动回归：

```bash
.venv/bin/pytest -q \
  tests/unit/test_replay.py \
  tests/unit/test_bili_selector.py \
  tests/unit/test_bili_peripherals.py \
  tests/unit/test_director.py \
  tests/unit/test_proactive.py \
  tests/unit/test_distill.py \
  tests/unit/test_stage3_acceptance.py \
  tests/unit/test_mvp_validation_set.py
```

提交前仍以完整门禁为准：

```bash
scripts/gate.sh
```

## 11. 放行标准

进入真实房间影子运行前，必须同时满足：

- G0 全绿。
- 沙箱 20 次打断至少成功 19 次，10 分钟机械噪声误回复 0 次。
- 付费事件测试零漏、零重复完整答谢、零金额或用户名串线。
- 紧急叫停 100% 止声。
- 六位候选业务测试中，准备进入真实内测的那一位总平均至少 4.0，且无 P0/P1。

把 AI 声音送进真实直播前，还要额外满足：

- 同一候选完成 60 分钟影子运行。
- 主播知道紧急叫停位置，并亲手成功演练两次。
- 主播确认伴播的人设、称呼、禁区和声音路由。
- 已知问题清单明确写出多人声、画面不可见、外放闭麦不能打断等边界。

## 12. 后续真实业务效果验证

沙箱通过后，真实效果用同一主播自己的基线做对照，避免拿不同体量主播横比。建议每位主播至少
做六场、每场 60 分钟：三场伴播关闭、三场伴播开启，时段和内容类型尽量配对，顺序交错。

主指标按每观众小时归一化：有效弹幕数、发言观众数、AI 发言后 60 秒内新增弹幕、超过 90 秒
的冷场次数。辅助指标是主播打断率、紧急叫停次数、主播主观负担和愿意继续使用的比例。停留时长、
关注和礼物收入必须从 B 站主播后台取数；当前事件流没有完整离房事件，不能用本地库伪造停留。

六场只能作方向判断。要宣称业务提升，还需要扩大主播数、控制内容和时段差异，并报告置信区间；
在那之前，结论应写“观察到改善迹象”，不能写成确定因果。

## 历史回归记录的出处

第 13 到 21 节是 `yiko/problem-update` 分支上的历次回归记录，随本方案一起搬来，按原样保留。
里面的助手名（Mia/Miya）、测试数量和「默认/改动人设」页描述的都是那条分支当时的状态；
第 13 节提到的双方案切换页在本分支被四人设助手页取代，第 18 节提到的专项回归入口
`scripts/test_recent_changes.sh` 没有随移植带入——本分支的回归入口只有 `scripts/gate.sh`。
本分支自己的移植回归见第 22 节。

## 13. 2026-08-22 联动回归记录

这次回归覆盖控制中心五个页面、配置热更新、两套测试集和正式后端链路：

- 完整门禁通过：1039 个单元测试、20 个 s2s 集成测试、26 个 Chromium 界面测试；另有
  3 个单元测试和 4 个集成测试按环境条件明确跳过，没有把跳过写成通过。
- 功能验收 18 张、业务测试 36 张，共 54 张卡全部经过 `MockTestRunner → QueueSource →
  Assembly/Selector`；付费优先级和弹幕洪峰代表用例继续走到 `Scheduler → S2SLink →
  MockRealtimeServer`。覆盖代码在 `tests/unit/test_testsets_backend.py`。
- 真实 DashScope 实例中切换 Mia 的默认/改动人设时，进程 PID 和界面 URL 未变化，模型上下文
  字数按 593 → 1098 → 593 更新；没有 `link_down`、重连或会话轮换。编辑 identity 后保存也
  立即触发上下文刷新，随后已恢复测试前文本和默认人设。
- 真实「测试」页运行 `func.danmaku.select` 和 `biz.bettergi.risk-boundary`，共 7 个事件进入
  Assembly，产生 3 个意图；调度队列最终清空，事件循环无告警。两张卡都完成注入并进入人工
  判断阶段。

自动回归只能证明链路和规则执行。业务卡的语言质量仍按卡片标准人工判断；本轮 BetterGI 样本
守住了“不保证零风险、不提供绕检测方法”的边界，但对工具工作方式解释不足，不能据此把该卡
标成业务效果通过。

## 14. 2026-08-24 直播信息、欢迎和弹幕策略回归

- 完整门禁通过：1084 个单元测试、20 个 s2s 集成测试、28 个 Chromium 界面测试；另有 3 个
  单元测试和 4 个集成测试按环境条件明确跳过。格式检查、静态检查和全量严格类型检查均通过。
- 系统页新增「直播主题简介」，主播昵称和简介改为点击「保存直播信息」后一起落盘并热更新。
  保存不重连语音服务；直播简介在动态上下文中有独立段落，普通欢迎和 VIP 欢迎都会读取。
- 普通欢迎的模型事件里不再携带批次人数，提示词同时禁止播报、暗示和猜测人数。VIP 由平台
  `ENTRY` 的舰队等级或本房五级以上粉丝牌提升，舰长、提督、总督和高等级粉丝牌使用不同欢迎
  强度，不从历史送礼记录猜身份。
- 弹幕策略改为先说明「谁问了什么」，再由 Mia 用自己的知识和判断给有效回答。只有未公开计划、
  私事和个人承诺等确实未知的信息才请主播补充，禁止用「我是伴播」「我可不敢」反复推脱。
- 功能验收增至 22 张，业务测试保持 36 张。功能集新增自主回答、动态进房、四档重点观众身份和
  两段式弹幕价值过滤，暂停用例
  改为当前顶层暂停/自动恢复；功能集与业务集不再直接注入内部 `vip_enter`，统一从平台进房身份
  经过正式 Assembly 提升。直播 Mock 合并成一个「进房欢迎」开关，并展示完整身份、礼物电池、
  原始金额和回复引用；金额只用于监控核对，模型提示与回复仍禁止输出。
- 默认语音输出不再固定在进程启动时的设备。macOS 中途连接 AirPods 或切换默认扬声器后，后台
  检测会停止旧 PortAudio 回调并重建新流；显式 `--output-device` 仍按用户选择固定，不擅自切换。

## 15. 2026-08-27 动态直播事件节奏回归

这次把直播间事件的固定冷却改成了按房间活跃度动态调节。该机制只观察 B 站直播间事件，不把
主播麦克风语音计入活跃度；主播开口仍能立即打断普通直播事件回复。弹幕、普通进房和主动话题
共用普通事件预算，礼物、SC、上舰和重点观众进房不消耗该预算。

完整门禁通过：1122 个单元测试、20 个 s2s 集成测试、29 个 Chromium 界面测试；另有 3 个
单元测试和 4 个集成测试按环境条件明确跳过。格式检查、静态检查和全量严格类型检查均通过。
功能验收 22 张、业务测试 36 张都经过正式 `Assembly/Selector` 链路回归。

使用 `tools/simulate_event_pacing.py` 对每档话痨程度连续模拟 3 分钟。下表数字为伴播获得的
发言机会数，不包含模型文案质量：

| 话痨程度 | 无人，0 条/分 | 冷清，2 条/分 | 稀疏，6 条/分 | 活跃，18 条/分 | 繁忙，60 条/分 |
|---|---:|---:|---:|---:|---:|
| low | 4 | 6 | 9 | 7 | 3 |
| medium | 5 | 6 | 16 | 11 | 5 |
| high | 8 | 12 | 18 | 17 | 7 |

无人房间的 4/5/8 次全部来自主动话题；medium 在冷清房间回复了 6/6 条弹幕，在繁忙房间只给
180 条弹幕提供 5 次回复机会。三档在繁忙房间的回复占比分别约为 1.7%、2.8% 和 3.9%，主动
话题均被关闭。因此从触发频率看，已经达到“无人时主动暖场、讨论热烈时减少插嘴”。这只是确定性
策略模拟；回复是否自然、话题是否值得接，仍要在测试页按业务卡人工判断，真实直播间阈值也要继续
观察后再校准。

本轮只选择性吸收 PR #1 的指定 Prompt 规则：明确 Mia 只与主播建立搭档关系；单条弹幕转述
「谁问了什么」，多人同诉求不点名；普通和中额礼物围绕礼物名自然接梗，高额礼物强调惊喜、
重视且不承诺回报；VIP 可点名并按粉丝牌、舰长、提督、总督分档，只有可靠上下文证明来过或
有共同经历时才表达想念或说「欢迎回来」。未引入 PR 的重复弹幕时间规则、低信息弹幕调侃、
批量进房话术、固定礼物敬称和示例句，也没有修改主动话题 Prompt。

## 16. 2026-08-27 输入来源 Prompt 路由回归

本轮把「共享上下文」与「当前回合输入规则」分开。主播语音和直播事件继续使用同一个 Realtime
Session、同一份 Conversation、记忆、直播简介和本场进展；没有创建语音会话和事件会话两套
历史。公共人设只保留 Mia 的稳定身份、性格、语言风格以及与主播的长期关系。

麦克风隐式回合只追加主播语音规则，明确当前说话人是主播，以及「我」「你」的指代。弹幕、
礼物、SC、上舰和进房的显式回合使用同一份公共上下文，但只追加观众事件规则和本次事件要求；
它们不会携带「当前输入是主播语音」的规则，也不会通过 `session.update` 临时切换 Prompt。
主动话题只使用公共上下文和主动话题要求，不套用任何说话人关系。

事件输入和完成的事件回复都会进入同一份对话历史，随后主播语音可以继续引用；取消、失败和
超时回复不写回。新增功能验收 `func.voice.event-shared-context`，人工检查语音→弹幕→语音顺序中
的上下文承接与说话人识别。完整门禁通过：1129 个单元测试、20 个 s2s 集成测试、29 个
Chromium 界面测试；另有 3 个单元测试和 4 个集成测试按环境条件明确跳过。格式、静态检查和
严格类型检查均通过，功能验收增至 23 张，业务测试保持 36 张。

## 17. 2026-08-27 主动话题破冰 Prompt 调整

正常时主动话题保持「侧路挑候选话题 → Realtime 按 Mia 人设说出」的两段链路。两段 Prompt 都新增
当前时间和连续无人回应次数，并要求优先延续未完成话题；需要破冰时，先给出 Miya 自己的观察，
再向整个直播间抛低门槛开放问题，不只点名追问主播。主播语音、观众弹幕或 SC 会把连续无回应
计数清零；无回应时下一次换角度、降低参与门槛，不复读和责怪观众。

侧路模型未配置、超时或报错时，动态冷场阈值和调度优先级不变，但跳过候选预生成，由 Realtime 直接读取
共享上下文、直播简介、本场进展和记忆选题并说出。侧路恢复后的后续轮次会自动回到两段链路，不重连 Realtime。

完整门禁通过：1131 个单元测试、20 个 s2s 集成测试和 29 个 Chromium 界面测试；另有 3 个
单元测试和 4 个集成测试按环境条件明确跳过。新增回归覆盖首次主动开口、连续无回应累加、主播或直播互动后清零、
当前时间与历史对话进入候选生成材料，以及无侧路和侧路失败时仍提交 Realtime 主动回复。

## 18. 2026-08-27 两日改动专项测试集

本轮将近两天的改动按四类重新对账，并把人工卡和自动断言绑定到同一份专项回归入口
`scripts/test_recent_changes.sh`。专项回归共 283 项，全部通过；其中 25 张功能卡和 36 张业务卡
均进入正式 `Assembly/Selector` 后端链路，代表性冲突和压力卡继续进入 Scheduler 与本地 Mock
Realtime，不只检查 JSON 能否读取。

完整门禁通过：1134 个单元测试、20 个 s2s 集成测试和 29 个 Chromium 界面测试；另有 3 个
单元测试和 4 个集成测试按环境条件明确跳过。Black、Ruff、全量严格类型检查、CLI 冒烟和
profile 覆盖层全部通过。

| 改动类型 | 具体检查点 | 测试页卡片 | 自动回归 |
|---|---|---|---|
| Prompt 调整 | 公共人设与语音/事件回合规则分开，但共享同一段历史；弹幕先定位原问题再独立回答；VIP、礼物、上舰按事件身份和分档表达；主动话题使用当前时间、连续无回应次数、最近对话与事件，侧路失败时仍由 Realtime 降级生成 | `func.voice.event-shared-context`、`func.danmaku.agency`、`func.entry.vip-tiers`、`func.gift.tiers`、`func.guard.buy`、`func.proactive.cold` | `test_persona.py`、`test_assembly.py`、`test_director.py`、`test_hosted_link.py`、`test_proactive.py` |
| 动态触发频率 | 只统计直播事件，不统计麦克风；冷清/稀疏/活跃/繁忙四档动态改变弹幕窗口、普通预算、进房合并和主动话题阈值；普通弹幕、进房、主动话题共用预算，付费与重点观众绕过；主播语音仍拥有最高说话权 | `func.danmaku.select`、`func.entry.batch`、`func.entry.chat-suppression`、`func.proactive.cold`、新增 `func.pacing.busy-room` | `test_event_pacing.py`、`test_testsets_backend.py` |
| 弹幕过滤 | 明确提问、点名、纠错、故障、请求和上下文相关短句直接进入候选；纯数字、表情、低信息词和重复字符直接过滤；跨用户近似重复两分钟内只保留第一条；其余再按文本与身份打分，门槛随话痨程度和房间活跃度调整 | `func.danmaku.local-value-filter`、`func.danmaku.select` | `test_bili_selector.py` |
| 忽略主播弹幕回复 | 房间初始化后用房主 UID 标记主播普通弹幕；事件仍进入流水、记忆、蒸馏和 Realtime 共享上下文，只在回复分流前截断；UID 为 0 的掩码观众不误伤，房主礼物、SC 等其他行为不受影响 | 新增 `func.danmaku.anchor-ignore`，需真实直播间 | `test_bili_translate.py`、`test_assembly.py`、`test_bili_peripherals.py` |

新增的繁忙房间卡在 5 秒内注入 31 条弹幕，随后注入一次普通进房和一条 SC。自动断言确认
`activity=busy`、弹幕窗口为 8 秒、普通进房被抑制、主动话题关闭，同时 SC 仍进入回复链路。
主播弹幕卡不在 mock 层伪造房主身份；自动层验证源头标记、观察链路与回复截断，人工层使用真实
房主账号和另一位观众做对照，避免测试到了错误的架构层。

## 19. 2026-08-27 音频连续性、历史协议与欢迎文案回归

本轮定位到音频卡顿发生在本地播放端：DashScope 的音频增量以突发方式到达，而旧回调收到短包后
立即播放，再把同一播放块的剩余部分补成静音，听感就是词内断续。运行进程的 CPU 和内存没有达到
瓶颈，当前 AirPods 输出也支持 24 kHz 单声道，因此不是模型格式不兼容。播放端新增 120 ms 起播
缓冲；生成未结束却暂时断粮时保留短包并重新缓冲，生成完成后仍会排空不足一个块的尾音。健康状态
同步外显重缓冲、欠载、PortAudio 回调状态和丢弃时长，便于长场继续定位。

`assistant role only supports content type 'output_text', got 'input_text'` 的原因是已完成事件回复写回
共享历史时，provider 不分角色一律构造 `input_text`。现在 user 历史使用 `input_text`，assistant
历史使用 `output_text`；完成的事件回复能正常进入共享历史，取消、失败和超时回复仍不写入。

弹幕提问要求先用短话重复问题原意和关键名词，再直接回答。普通和 VIP 进房均不强制介绍直播内容；
最近已经介绍过时优先只做简单欢迎，并对照最近三次进房回复避免相同开头、固定句式和
“欢迎某某，咱们正聊着……”模板。可直接叫昵称、说“来啦”或使用不带“欢迎”二字的轻量招呼。

完整门禁通过：1139 个单元测试、20 个 s2s 集成测试和 29 个 Chromium 界面测试；另有 3 个
单元测试和 4 个集成测试按环境条件明确跳过。Black、Ruff、全量严格类型检查、CLI 冒烟和
profile 覆盖层全部通过。新增回归覆盖两种 provider 的角色内容类型、音频抖动预缓冲、短尾音
排空、弹幕问题转述，以及普通/VIP 进房的内容去重和欢迎句式变化；近期改动专项回归 286 项也
全部通过，25 张功能卡和 36 张业务卡继续进入正式 Assembly/Selector 后端链路。

## 20. 2026-08-27 主播弹幕观察链路回归

主播普通弹幕不再在 B 站事件源直接丢弃。事件源依据房间初始化返回的房主 UID 给事件加
`is_anchor` 标记，控制中心流水显示“主播本人”；Assembly 先把事件交给 MemoryStore 和
Distiller，并以 `[主播弹幕]` 观察项写入当前 Realtime Conversation，随后在进入 Selector、普通
事件预算和 Scheduler 以前返回。因此后续主播语音、观众事件和主动话题可以引用主播发过的弹幕，
但这条弹幕本身不会触发 Miya 回复。

匿名 UID 仍不会误判为主播，房主礼物、SC 等非普通弹幕事件也不受截断影响。完整门禁通过：
1140 个单元测试、20 个 s2s 集成测试和 29 个 Chromium 界面测试；另有 3 个单元测试和 4 个
集成测试按环境条件明确跳过。近期改动专项回归 287 项全部通过。

## 21. 2026-08-27 主播说话期间的弹幕保留与用户冷却移除

原链路会在主播仍说话时照常选出弹幕并调用 Assembly：普通事件预算已经扣除，调度意图的有效期
也已经开始计时。弹幕随后卡在说话权闸门，主播说得稍久就以过期结束；Assembly 因预算不足提前
返回时，Selector 仍把这次回调当成交付成功。两种情况都会让有意义的弹幕看起来像被直接略过。

现在每个动态窗口仍只保留一位胜者。主播说话或普通弹幕预算暂不可用时，胜者进入最多 5 条的
候选池，不调用 Assembly，所以不扣预算，也不创建 Intent 或启动有效期。主播停下且预算可用后，
只释放候选池中分数最高的一条；同分时取更新的一条，其余记为 `selection.lost_deferred`，避免形成
旧弹幕积压。主动话题会把候选池计入“已有普通事件等待”，不会同时抢着开新话题。

“该用户刚被回复过”不再是过滤条件：运行时的 60 秒单用户冷却、配置字段和前端元数据均已移除。
旧本地配置若仍带 `interaction.danmaku.per_uid_cooldown_s`，加载器会只忽略这个已退役字段，避免
升级后无法启动；同一观众的新问题继续按内容价值、窗口竞争和普通事件预算处理。

测试页新增 `func.danmaku.same-viewer-follow-up` 和 `func.danmaku.defer-during-speech`，功能卡总数
增至 27 张。自动回归覆盖：同一用户连续窗口可再次胜出、主播说话期间多窗口候选不交付、停下后
只释放最优一条、预算不提前扣除、Intent 有效期不提前启动，以及旧配置兼容。近期改动专项回归
315 项全部通过。完整门禁通过：1143 个单元测试、20 个 s2s 集成测试和 29 个 Chromium 界面
测试；另有 3 个单元测试和 4 个集成测试按环境条件明确跳过。Black、Ruff、全量严格类型检查、
CLI 冒烟和 profile 覆盖层全部通过。

## 22. 2026-08-28 功能移植到 yiang/dev/preview 的回归

`yiko/problem-update` 的功能点在 `yiang/dev/preview` 上重新实现完毕（不是合并——两条分支的
音频、Provider、人设和配置结构都已分叉，逐个功能按本分支架构重写）。与历史记录相比的
行为差异：

- 「紧急叫停」和「暂停」并存：叫停只掐当前回复和队列，不碰麦克风；暂停是总闸，停事件
  入口、闸麦克风、挂起语音连接，恢复按严格反序执行。历史记录里删掉叫停只留暂停的做法
  没有采纳。
- 助手页是四个随包人设（tofu/butter/hanako/ming）的直播中切换加锚点编辑，不是单助手的
  默认/改动双方案。切人设只换提示词，皮肤和音色跟配置走。
- 通用配置页降级为「高级」页保留，系统页是日常入口；健康卡也挪到高级页。
- 浏览器音频（直播 Mock）走既有二进制音频通道的 `?role=mock` 分支，复用随包采集
  worklet，不再用 base64 控制帧传音频。

本方案正文（第 1 到 12 节）已按本分支行为校订：优先级和门槛的 file:line 逐条重查过，
面板页面名换成六页布局，控制名换成「紧急叫停」。原先四处「待验证」已于 2026-08-29
真机验证完毕：①火山挂起/恢复接续对话、改名与换人设直播中生效（顺带实证 O2.0 的
UpdateConfig 载 bot_name 无效，adapter 已改为换会话）；②DashScope 挂起恢复前后判停
与回复均完整闭合（真人声波形驱动），噪音门对真实波形定量合理（真人声 96% 放行、
键盘突发 68% 替静音、纯静音全吞）——主观「手感」仍留给主播戴上真麦克风感受；
③④直播 Mock 用真 Chrome 抓带音轨标签页、连正在直播的真实房间跑通全链路，模型真的
听见了标签页音频并多轮回复。

完整门禁通过（2026-08-28 实跑）：1696 个单元测试、21 个 s2s 集成测试（另有 3 个按环境条件
明确跳过）、49 个 Chromium 界面测试；Black、Ruff、全量严格类型检查（含 Windows 平台复查）、
CLI 冒烟、profile 覆盖层和 eslint 全部通过。
