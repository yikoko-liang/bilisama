# 本地版本回退记录

已回退到 2026-09-11 23:47:59（北京时间）那次答复结束时的本地代码：新增 15 段 Seed TTS 2.0 台词，全部 84 个语音步骤检查通过。

目标答复 ID：`msg_08b397f4c40bba48016aa422ad27e4819ba22941bf31045457`。

## 回退依据与范围

目标改动当时没有提交到 Git，因此没有使用最近一次提交代替目标版本。
根据本任务保存的文件变更记录，逐轮反向还原目标答复之后的改动。18 个既有文件恢复到目标状态；后续新增、当前仍存在的 8 个文件已移出工作版本，均可从备份恢复。中间曾创建、后来已经删除的文件保持不存在。

还原时三处 Python 排版差异作了核对，其中涉及括号排版的一处通过语法树比较确认含义一致。还原后逐文件核对内容哈希及应删除文件的状态；检查通过。回归记录恢复到 [验证计划第 28 节](../../docs/mvp-validation-plan.md:807)。

保留目标时点已有的简单 14 例、困难 27 例测试集。没有删除音频缓存、运行日志、用户记忆、凭据、附件或无关文件。分支仍为 `yiko/preview_testset`，未提交、未推送、未移动分支指针。

## 本轮验证

`scripts/gate.sh` 返回 0：

- 单元测试：2227 项通过，4 项因环境条件跳过。
- 集成测试：20 项通过，6 项因环境条件跳过。
- 浏览器测试：82 项通过。
- Black、Ruff、两平台严格类型检查及 `git diff --check` 通过。
- 上游 S2S 字段对账因缺少检出目录跳过；ESLint 因未安装跳过。

这些结果验证了恢复后的自动化测试，不代表所有真实模型坏例已经解决。本轮未重启正在运行的服务，旧进程需重启后才加载回退代码。

## 文件

- [完整 Prompt](all-prompts.md)：从回退后的加载器、构造函数及模板导出；包含当前豆腐人设、语音意图、事件回合、各事件要求、回复长度各档、主动话题及降级、后台记忆整理、点击互动和其他随包角色模板。动态上下文用占位符展示，不导出真实记忆或密钥。
- [回退前完整文件备份](/Users/yikoliang/Desktop/bilisama-rollback-20260914.Ogz6Pr/before-rollback.tar.gz)。
- [回退文件清单与目标哈希](/Users/yikoliang/Desktop/bilisama-rollback-20260914.Ogz6Pr/manifest.json)。
- [本轮门禁日志](/Users/yikoliang/Desktop/bilisama-rollback-20260914.Ogz6Pr/gate.log)。

重建脚本、Prompt 导出脚本和回退前补丁均保留在上述备份目录，不加入应用代码。此记录及 Prompt 是本次新增的交付文件，不参与服务运行。

## 后续归档：2026-09-14

按用户要求，将上述回退版本的 50 个源码、测试和文档文件提交为 `0d103a9592286c2bbba9a2be2a0ea5197f2897ee`，推送到内部 GitLab 的 `yiko/planner_update` 新分支，已核对远端提交哈希一致。
本地当前分支切换为 `yiko/planner_update`；原有 `yiko/preview_testset`、`yiko/mvp-control-center` 及远端 `yiang/feat/planner` 的指针未变。
推送前再次运行完整门禁，返回 0：2227 项单元、20 项集成、82 项浏览器测试通过；环境跳过项同上。
未提交本机附件、音频缓存、凭据或 `outputs/` 导出文件。本次归档没有继续修改功能或 Prompt，也没有重启服务。
