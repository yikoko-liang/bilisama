# BiliSama 项目约定

## 指导原则（需求文档 §1.4，优先级高于「尽快交付」）

以瞎猜接口为耻，以认真查询为荣。以模糊执行为耻，以寻求确认为荣。
以臆想业务为耻，以人类确认为荣。以创造接口为耻，以复用现有为荣。
以跳过验证为耻，以主动测试为荣。以破坏架构为耻，以遵循规范为荣。
以假装理解为耻，以诚实无知为荣。以盲目修改为耻，以谨慎重构为荣。

落到日常：接口和上游行为先查代码再下结论，结论带 `file:line` 出处；拿不准业务
意图就问，不替用户拍板；能复用的不新造（如无必要勿增实体）；改完必须跑门禁；
没验证过的事写「待验证」，不写成事实。

## 代码规范（计划 §1.4 的十六条，这里挑动手前最容易忘的）

- 类型标注是硬门禁：`mypy --strict` 扫 `src tests tools` 全量，公共函数必须标注。
- 函数短小、单一职责；模块边界清楚——L3 不碰协议、L2 不碰产品策略（§3.7）。
- 错误处理要具体：不写裸 `except Exception`，保留异常上下文（`from exc`），
  不静默吞错；用户看到的报错给人话和可点的修复动作，不给 traceback。
- I/O、网络要有超时和资源释放；库代码用 logging 不用 print（CLI 除外）。
- 测试覆盖正常路径、边界和错误路径三类，不只测 happy path；修 bug 先写复现
  测试（先红后绿），而且要亲眼看着它红。
- 配置、密钥、路径不硬编码：唯一真相源是 `bilisama.toml`，密钥永不入库。
- 改之前先理解现有结构；改动范围最小；不顺手重构无关代码；行为变更和纯重构
  分开提交，正文写明哪种。
- 完整清单和逐条解释见计划文档 §1.4。

## 文风：所有回复和落盘文档

给用户的每一条回复、写进仓库或计划的每一份文档，都按下面的标准写。这条规则进 CLAUDE.md
就是为了不随上下文增长被忘掉——它已经被打回过三次。

- **像人话的中文。** 读者是中文工程师和主播，写完自己念一遍，念着别扭就重写。
- **半角逗号不当破折号。** 「这一处不修,验收就是错的」是机器翻译的痕迹——该用「——」
  就用「——」，该断句就断句。
- **黑话首次出现给一句大白话。** 变异测试、固定（pin）、保真度、对照组这类词，
  第一次出现时用一句话解释，或者直接换成平实说法。
- **不写 AI 腔。** 金句收尾、排比连发、"不是 X 而是 Y"句式连用、正文粗体滥用、
  空泛程度词（"显著""极大"）都算。一段最多一处强调。
- **事实一个不能丢。** 改措辞不许丢数字、出处（file:line）、限定词。

## 流程纪律

- **做完即回写。** 关掉一条待办、完成一个阶段、修正一个结论，当轮就更新计划文档里
  对应的状态（进度在 §15，欠账在 §16.8）。修正只进 commit 不进文档等于没改——
  这个学费付过一次：三处修正躺在 commit 里，文档还在教人做已经做完的事。
- **重要节点的用法进 runbook。** 新的可运行能力（服务、命令、测试方式）落地当轮
  写进 [docs/runbook.md](docs/runbook.md)，别让"怎么跑"只活在对话记录里。
- **README 是文档索引。** 新增重要文档时在 [README.md](README.md) 的索引表挂号。

## 代码语言分界

注释、docstring、commit message 用英文；CLI 输出、报错提示、设置界面文案、人设文本、
测试数据里的弹幕用中文。完整规则和理由见 [CONTRIBUTING.md](CONTRIBUTING.md)。

## 提交前

```bash
scripts/gate.sh
```

装了 s2s 的话它连集成层一起跑；没装会明说跳过了哪层，不会假装全过。

## 密钥

`path.sh`（本地 LLM 凭据）已在 `.gitignore` 里，永不入库。任何 key 只走环境变量。
