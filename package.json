{
  "name": "bilisama-js-gate",
  "private": true,
  "version": "0.0.0",
  "description": "Lint tooling for the JavaScript that ships (pet page, Electron shell). Not a build: nothing here is bundled or published.",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.9.1",
    "globals": "^17.11.0"
  }
}
